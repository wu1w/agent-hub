# 第六轮修复及整体审计结果

日期：2026-09-17。项目：`/Users/william/agent-hub`。

## 结论

用户指出的三条回归均已修复，并将原复现操作转换为回归测试。本轮又修复了整体复核发现的关联问题。最终验收以本次源文件、隔离夹具和自动化验证为边界：没有在这些检查中留下失败用例，不能据此保证所有客户端、任意故障时序或真实模型会话都无问题。

## 三条修复的实际行为

1. **坏 Skill YAML 不再阻断 Vault 撤权。** Vault 正文及授权接口只依赖 Vault 和必要投递，返回新 revision 与 Vault 专属数据。全局 snapshot 不在 Vault 事务内。测试同时断言 HTTP 200、实际授权为空、受控取值被拒绝、目录撤去条目、新 revision 可继续保存。投递失败回滚的原有测试仍通过。
2. **显示密钥不再覆盖草稿字段标记。** 服务端明文用于还原占位符；同 revision 的草稿保留自定义 secretFields。测试执行真实前端“标记→显示→隐藏→保存”事件链，最后检查密文解密后的字段仍为 secret；revision 冲突保留完整草稿。
3. **Vault 暂不可用不再覆盖历史交接。** 索引在写数据库前要求可用的秘密材料，否则返回 503。HTTP/CLI 展示及交接启动同步暂停。测试在错误密钥期间多次调用入口，逐次比较数据库和交接文件字节；恢复后重建索引，原交接仍完整。

历史交接清理移到独立的 `npm run hub -- scrub-handoffs`。操作前检查 Vault，可用时对目标交接进行事务化清理；遇到后续文件写入失败，所有已修改文件回滚。普通 `index` 不会隐式调用它。

## 整体复核发现并处理的关联问题

- 坏 Skill 还会阻断初次页面加载。snapshot 增加 Skills 独立不可用状态和告警，Vault 等模块保持可用；UI/CLI 不把扫描失败解释成“零资产/无冲突”。
- 显示请求迟到可能在失焦后重新显示秘密；慢速载入可能覆盖更新的本地草稿。增加请求代次检查及 Vault 操作中的编辑保护，测试覆盖失焦和慢载入时序。
- 顺序扫描中后续 Agent 失败可能留下前面的索引更新。改为先收集来源，再用 SQLite 事务提交更新；故障注入断言旧索引记录保持不变。
- 秘密值与条目 ID 相同时，全字符串替换会破坏掩码 Markdown 的条目身份。掩码现在按字段结构渲染，原 ID 和字段名保持不变，保存回环测试通过。
- 事务已提交但备份清理失败可能向调用方报保存失败。提交后清理改为尽力执行，下次写入前清理残留。恢复材料缺失则停止恢复、保留现有目标和 journal；故障注入验证不会误删唯一副本。

## 审计范围与证据

对照原始 A01—A15 报告及 PRD/SPEC，复核数据写入/回滚、路径白名单、Git 跟踪保护、Skill 目标和嵌套软链、Vault 掩码/授权/并发、前端草稿和请求时序、索引/交接、HTTP 认证与请求解析、CLI 参数和启动参数引用。已有相关测试全部保留执行；两处旧测试预期调整为新的“拒绝重建且保留数据”和“显式清理交接”契约，没有通过跳过用例规避失败。

- `npm test`：151 项通过，0 失败、0 跳过；较上一轮新增 13 项。
- `npm run typecheck`：通过。
- `node --check web/app.js`、`node --check web/vault-draft.js`：通过。
- `npm audit --json`：报告 0 条已知漏洞，含生产和开发依赖；这不等于应用代码无漏洞。
- Node：v22.18.0。测试使用临时 HOME/Hub、合成密钥、随机独立 HTTP 端口、真实前端函数的 Node VM/DOM 替身，以及文件故障注入。
- 新回归主要在 `src/core/round6-regressions.test.ts` 与 `src/server.test.ts`；验证摘要和源文件哈希见 `round6-verification.json`。

## 保留的边界

没有启动真实模型会话或执行真实浏览器点击验收；跨客户端原生规则实际消费仍待逐客户端验证。Own 不构成共享 AGENTS.md 的读取权限隔离。当前秘密材料无法识别已从 Vault 删除的历史秘密。

本次没有重启正式服务，也没有对个人 Hub 执行修复或清理命令。旧版本若已经把交接覆盖成提示文字，本次不会伪造或自动恢复原内容；恢复需要原始会话或独立备份。本次测试证明的是修复后的不可用/恢复路径保留原文件。
